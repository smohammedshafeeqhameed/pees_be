from django.urls import path
from . import views

app_name = 'myth_manager'
urlpatterns = [
    # path('myths/', views.SleepMythListView.as_view(), name='myth-list'),
    # path('myths/<int:pk>/', views.SleepMythDetailView.as_view(), name='myth-detail'),
    # Add more URL patterns as needed
]